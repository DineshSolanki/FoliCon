<UserControl x:Class="FoliCon.Views.OverlayStore"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:viewModels="clr-namespace:FoliCon.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance viewModels:OverlayStoreViewModel}"
             Background="{DynamicResource RegionBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <hc:Boolean2VisibilityReConverter x:Key="InvertedBoolToVis" />
    </UserControl.Resources>

    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Title" Value="Overlay Store" />
            <Setter Property="Height" Value="650" />
            <Setter Property="Width" Value="900" />
            <Setter Property="MinHeight" Value="400" />
            <Setter Property="MinWidth" Value="600" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid Margin="16,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search + Filter Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <hc:SearchBar Grid.Column="0"
                          Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          hc:InfoElement.Placeholder="Search overlays..."
                          hc:InfoElement.ShowClearButton="True"
                          Style="{StaticResource SearchBarPlus}"
                          IsRealTime="True"
                          VerticalAlignment="Center" />

            <ComboBox Grid.Column="1" Margin="8,0,0,0" MinWidth="120" VerticalAlignment="Center"
                      ItemsSource="{Binding AvailableTags}"
                      SelectedItem="{Binding SelectedTag}"
                      hc:InfoElement.Placeholder="Filter by tag" />

            <Button Grid.Column="2" Margin="8,0,0,0" Content="↻ Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ButtonPrimary}"
                    VerticalAlignment="Center" />
        </Grid>

        <!-- Overlay Grid -->
        <hc:BusyIndicator Grid.Row="1" IsBusy="{Binding IsLoading}"
                          BusyContent="Loading catalog...">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Overlays}" HorizontalContentAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Margin" Value="4" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="200" Padding="8"
                                       CornerRadius="4"
                                       Background="{DynamicResource SecondaryRegionBrush}"
                                       BorderBrush="{DynamicResource BorderBrush}"
                                       BorderThickness="1">
                                <StackPanel>
                                    <!-- Preview Image -->
                                    <Grid Height="120" Margin="0,0,0,6">
                                        <Image Source="{Binding PreviewImage}"
                                               Stretch="Uniform"
                                               RenderOptions.BitmapScalingMode="HighQuality" />
                                        <TextBlock Text="Loading..."
                                                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource SecondaryTextBrush}" />
                                    </Grid>

                                    <!-- Name + Author -->
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                               FontSize="13" TextTrimming="CharacterEllipsis"
                                               Foreground="{DynamicResource PrimaryTextBrush}" />
                                    <TextBlock Text="{Binding Author}" FontSize="11"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               Margin="0,1,0,0" />
                                    <TextBlock Text="{Binding VersionDisplay}" FontSize="10"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               Margin="0,1,0,4" />

                                    <!-- Action Buttons -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Install"
                                                Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsInstalled, Converter={StaticResource InvertedBoolToVis}}"
                                                Style="{StaticResource ButtonPrimary}"
                                                Padding="12,4" FontSize="11" />
                                        <Button Content="⟳ Update"
                                                Command="{Binding DataContext.UpdateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource ButtonPrimary}"
                                                Padding="12,4" FontSize="11" Margin="4,0,0,0" />
                                        <TextBlock Text="✓ Installed"
                                                   Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}"
                                                   VerticalAlignment="Center"
                                                   Foreground="Green"
                                                   FontSize="11" FontWeight="SemiBold" />
                                        <Button Content="✕"
                                                Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}"
                                                Style="{StaticResource ButtonPrimary}"
                                                Padding="6,4" FontSize="11" Margin="4,0,0,0"
                                                ToolTip="Uninstall" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </hc:BusyIndicator>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"
                       VerticalAlignment="Center" FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}" />

            <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"
                       Foreground="Red"
                       FontSize="11" TextTrimming="CharacterEllipsis"
                       MaxWidth="400" />
        </Grid>
    </Grid>
</UserControl>
