<UserControl x:Class="FoliCon.Views.PosterIconConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:modules="clr-namespace:FoliCon.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:langs="clr-namespace:FoliCon.Properties.Langs"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:extension="clr-namespace:FoliCon.Modules.Extension"
             xmlns:convertor="clr-namespace:FoliCon.Modules.Convertor"
             xmlns:sys="clr-namespace:System.Windows;assembly=PresentationFramework"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance modules:PosterIconConfigViewModel }">
    <UserControl.Resources>
        <ResourceDictionary>
            <convertor:String2BooleanConvertor x:Key="StringToBooleanConvertor" />
        </ResourceDictionary>
    </UserControl.Resources>
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="MinHeight" Value="300" />
            <Setter Property="SizeToContent" Value="Height" />
            <Setter Property="MaxHeight"
                    Value="{Binding Source={x:Static sys:SystemParameters.WorkArea}, Path=Height}" />
            <Setter Property="Width" Value="1266" />
            <Setter Property="MaxWidth"
                    Value="{Binding Source={x:Static sys:SystemParameters.WorkArea}, Path=Width}" />
            <Setter Property="WindowStyle" Value="SingleBorderWindow" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
        </Style>
    </prism:Dialog.WindowStyle>
    <GroupBox
        Style="{StaticResource GroupBoxTab}" FontSize="22" FontFamily="Cambria"
        hc:TitleElement.Background="{DynamicResource RegionBrush}"
        Foreground="Transparent" BorderThickness="0"
        Background="{DynamicResource RegionBrush}" HorizontalContentAlignment="Stretch" HorizontalAlignment="Center">
        <Grid Margin="15,0,15,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableOverlays}"
                              HorizontalContentAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Width" Value="200" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <RadioButton GroupName="PosterOverlay"
                                         Command="{Binding DataContext.IconOverlayChangedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                         CommandParameter="{Binding OverlayId}"
                                         hc:Poptip.Content="{Binding DisplayName}"
                                         hc:Poptip.Placement="LeftTop"
                                         Margin="0,4,8,4" FontSize="16"
                                         VerticalAlignment="Stretch"
                                         HorizontalAlignment="Left"
                                         IsChecked="{Binding IsActive, Mode=OneWay}">
                                <RadioButton.Content>
                                    <Image Source="{Binding DemoIconPath}" RenderOptions.BitmapScalingMode="HighQuality" />
                                </RadioButton.Content>
                            </RadioButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Browse Overlay Store..." Margin="0,0,8,0" Padding="12,4"
                        Command="{Binding BrowseOverlayStoreCommand}" />
                <Button Content="Import Local Overlay..." Margin="0,0,0,0" Padding="12,4"
                        IsEnabled="False" ToolTip="Coming in Release 2" />
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
