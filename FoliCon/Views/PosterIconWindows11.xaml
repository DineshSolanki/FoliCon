<data:PosterIconBase x:Class="FoliCon.Views.PosterIconWindows11"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:data="clr-namespace:FoliCon.Models.Data"
             prism:ViewModelLocator.AutoWireViewModel="False"  Width="256" Height="256"
             d:DataContext="{d:DesignInstance Type=data:PosterIcon, IsDesignTimeCreatable=True}">
    <Grid>
        <!-- Windows 11 style folder background with rounded corners -->
        <Border CornerRadius="8,8,8,8" Background="#FFE8C565" Margin="0,40,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#40000000" Direction="315" ShadowDepth="4" Opacity="0.5" BlurRadius="8"/>
            </Border.Effect>
        </Border>
        
        <!-- Folder tab at the top -->
        <Border CornerRadius="8,8,0,0" Background="#FFF7DC8F" Margin="10,20,156,201" />
        
        <!-- Poster image container with rounded corners -->
        <Border CornerRadius="6,6,6,6" Margin="15,60,15,35" ClipToBounds="True">
            <Border.Background>
                <ImageBrush Stretch="Uniform" ImageSource="{Binding FolderJpg}" />
            </Border.Background>
        </Border>
        
        <!-- Rating shield overlay -->
        <Grid Visibility="{Binding RatingVisibility}">
            <Ellipse Width="50" Height="50" Fill="#E0FFFFFF" Margin="186,171,20,35" />
            <Ellipse Width="46" Height="46" Fill="#F0F7DC8F" Margin="188,173,22,37" />
            <TextBlock Text="{Binding Rating}" HorizontalAlignment="Center"
                       VerticalAlignment="Center" FontFamily="Segoe UI" FontWeight="Bold"
                       FontSize="18" Margin="186,171,20,35" Foreground="#333333"/>
        </Grid>
    </Grid>
</data:PosterIconBase>
